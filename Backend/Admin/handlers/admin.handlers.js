import db from "../models/index.js";

const getTokenRequests = async (req, res, next) => {};

const approveToken = async (req, res, next) => {};

export default {
	getTokenRequests,
	approveToken,
};
